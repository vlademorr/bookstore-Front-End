import React from "react";
import BookListContainer from "../bookList";

const HomePage = () => {
    return (
        <div>
            <BookListContainer />
        </div>
    );
};

export default HomePage;